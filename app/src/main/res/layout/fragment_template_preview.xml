<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <android.support.v7.widget.Toolbar
        android:id="@+id/my_toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:background="?attr/colorPrimary"
        android:minHeight="?attr/actionBarSize"
        android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
        app:popupTheme="@style/ThemeOverlay.AppCompat.Light"/>
   <ImageView
       android:layout_width="wrap_content"
       android:layout_height="wrap_content"
       android:minWidth="30dp"
       android:minHeight="30dp"
       android:visibility="gone"
       android:id="@+id/img_nav_back"
       android:src="@drawable/ic_arrow_back"/>
  <FrameLayout
      android:layout_width="match_parent"
      android:layout_below="@id/img_nav_back"
      android:layout_height="wrap_content">
      <ImageView
          android:layout_width="match_parent"
          android:layout_margin="@dimen/dimen_4dp"
          android:src="@drawable/resume_one"
          android:layout_height="wrap_content" />
      <TextView
          android:layout_width="wrap_content"
          android:layout_height="wrap_content"
          android:layout_gravity="bottom|right"
          android:layout_marginBottom="40dp"
          android:text="@string/edit_resume"
          android:id="@+id/tv_editResume"
          android:textStyle="bold"
          android:textColor="@android:color/white"
          android:padding="@dimen/dimen_8dp"
          android:background="@drawable/rounded_corner_bg_left_primery_color"/>
  </FrameLayout>
</RelativeLayout>